package com.backoffice.BackOfficeSpring.generadorContrasenas;

import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;

public class GeneradorPassword {
// public static void main(String[] args) {
// // Se crea una instancia del encoder
// BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();

// // Contraseña en texto plano que deseamos encriptar
// String passwordPlano = "1234";

// // Se encripta la contraseña
// String hashedPassword = encoder.encode(passwordPlano);

// // Se muestra el hash en la consola
// System.out.println("Contraseña hasheada: " + hashedPassword);
// }
}
